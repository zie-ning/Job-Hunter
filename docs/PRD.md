# 채용지원 어시스턴트 — PRD

## 1. 개요

### 1.1 배경
개발자 취업을 준비하는 개인이 자신의 이력서를 여러 채용공고에 맞춰 반복적으로 다듬는 과정은 시간이 많이 들고, 어떤 수정이 실제로 효과가 있었는지 객관적으로 확인하기 어렵다. 이 프로젝트는 이 과정을 AI 에이전트 파이프라인으로 지원하는 서비스를 구축하면서, 동시에 **개발자 취업 포트폴리오**로 활용 가능한 결과물을 만드는 것을 목표로 한다.

### 1.2 목표
- 실제 채용공고(JD) 데이터를 근거로 한 이력서 첨삭 제공
- 이력서 버전 간 변화와 그 효과(매칭 점수 개선)를 추적
- AI 에이전트 파이프라인(공고분석 → 매칭/갭분석 → 첨삭)을 실제로 동작하는 형태로 구현
- 포트폴리오로서 기술 선택 근거를 면접에서 설명할 수 있는 수준의 완성도 확보

---

## 2. MVP 범위

| 기능 | MVP 포함 여부 | 비고 |
|---|---|---|
| 이력서 버전 관리 | ✅ (단순 CRUD) | 버전별 diff, 개선도 추적, JD별 브랜치 관리 포함. 브랜치 병합(머지) 기능은 제외 |
| 공고분석 에이전트 | ✅ | JD 원문을 구조화(요구역량/기술스택/우대사항 추출) |
| 매칭/갭분석 에이전트 | ✅ | 매칭 점수(벡터 유사도)는 자동 계산. 구체적 갭 분석(LLM)은 JD별 브랜치 화면에서 버튼 클릭 시 실행 |
| 첨삭 에이전트 | ✅ | RAG 기반, JD별 브랜치 화면에서 "첨삭하기/피드백 받기" 클릭 시 실행. 그 브랜치가 겨냥한 JD 1개만 근거로 사용 |
| 발송 로직 | ✅ (슬랙만) | 매칭 점수 임계치 초과 시 슬랙 웹훅으로 알림 + 인터랙티브 버튼("지원 준비하기") 포함. 이메일/카카오톡은 제외 |

---

## 3. 시스템 아키텍처 개요

### 3.1 에이전트 파이프라인 (데이터 흐름)

```
[원티드 API / 워크넷 공공데이터]
        │
        ▼
  공고분석 에이전트  ──▶  구조화된 JD (요구역량/기술스택/우대사항) 저장
        │
        ▼
  매칭 점수 계산 (벡터 유사도, 자동·저비용) ──▶  관련 JD N개 선정
        │
        ▼
  (매칭 점수 ≥ 임계치) ──▶  슬랙 웹훅 알림 (인터랙티브 버튼 "지원 준비하기" 포함)
        │
        ▼
  사용자가 "지원 준비하기" 클릭 ──▶  JD별 브랜치 생성 (이미 있으면 기존 브랜치로 이동)
```

구체적 갭 분석과 첨삭 피드백 생성(LLM 호출, 고비용)은 이 자동 파이프라인에 포함되지 않는다. JD별 브랜치 화면에서 사용자가 "첨삭하기/피드백 받기"를 요청했을 때만 실행된다 (3.3 참고).

**설계 원칙**: 첨삭 에이전트는 전체 JD DB를 직접 검색하지 않는다. 반드시 JD별 브랜치가 겨냥한 JD 1개만 근거 문서로 사용해, "이 이력서와 실제로 관련 있는 공고"를 근거로 첨삭한다. 갭 분석·첨삭은 자동 실행되지 않고 사용자가 브랜치 화면에서 요청할 때만 실행되는데, 이는 LLM 생성 비용이 사용자 수 × 요청 빈도로 누적되는 지점이기 때문이다 (반면 공고분석·매칭 점수 계산은 JD당 1회 또는 순수 벡터 연산이라 자동 실행해도 비용 부담이 적다).

### 3.2 이력서 버전 관리 흐름

```
PDF/문서 업로드 → 텍스트 파싱 → 에디터에서 구조화된 형태로 수정 → 저장 시 코멘트(선택) 입력 → 버전 생성
                                                                                      │
                                                                                      ▼
                                              버전 목록 = 코멘트 + 매칭 점수 변화("개선도")가 1차 화면
                                                                                      │
                                                                                      ▼
                                          (상세보기 진입 시) 이전 버전과 텍스트 diff 확인 가능
```

PDF는 바이너리라 버전 간 비교가 어렵기 때문에, 업로드 시점에 파싱하여 구조화된 텍스트로 변환하고 이후 수정은 에디터에서 이루어진다. 각 저장 시점이 하나의 버전이 된다.

버전 목록의 1차 UX는 텍스트 diff가 아니라 git 커밋 메시지 같은 짧은 코멘트다. 이력서는 코드가 아니라 산문형 텍스트라서, 문단을 살짝 재배치하기만 해도 줄 단위 diff가 지저분해지고 "무엇을, 왜 바꿨는지"라는 의도를 전달하지 못한다. 코멘트는 그 의도를 바로 전달하고, 개선도(7번)의 점수 변화와 짝지어졌을 때 "이렇게 고쳤더니(코멘트) 이만큼 좋아졌다(점수)"는 서사를 만든다. 텍스트 diff는 정확한 문구 변경을 검증하고 싶을 때를 위해 상세보기에서 보조 기능으로 계속 제공한다.

### 3.3 JD별 브랜치 흐름

```
슬랙 알림의 "지원 준비하기" 클릭 (Slack Interactivity 콜백)
        │
        ▼
  해당 JD(소스+원본 공고 ID)에 브랜치가 이미 있는가?
        │                                   │
       없음                                 있음
        │                                   │
        ▼                                   ▼
  base 버전 선택 → 브랜치 생성          기존 브랜치로 이동
        │
        ▼
  브랜치 내에서 이력서 수정/저장
        │
        ▼
  브랜치가 겨냥한 JD 1개 대상으로 매칭 점수 재계산 (자동, 저비용)
        │
        ▼
  버전 간 점수 변화 = "개선도"로 추적 (7번 참고)
        │
        ▼
  사용자가 "첨삭하기/피드백 받기" 클릭 (온디맨드)
        │
        ▼
  구체적 갭 분석 + 첨삭 피드백 생성 (LLM, 4.3/4.4 참고)
```

브랜치는 이력서 버전 이력을 JD 단위로 분기해서 별도로 관리하는 단위다. 하나의 JD(소스+원본 공고 ID 기준)에는 브랜치가 최대 1개만 존재하며, 상태 필드(진행중/지원완료/마감)로 생애주기를 관리한다. 브랜치 간 병합(머지)은 지원하지 않는다 — 각 브랜치는 독립적인 이력으로 남는다.

---

## 4. 기능 명세

### 4.1 이력서 버전 관리
- PDF/문서 업로드 → 텍스트 파싱 → 에디터 UI에서 편집
- 저장 시 새 버전 생성 (기존 버전은 불변), 코멘트(선택, 빈칸 허용) 입력 가능
- 버전 목록의 1차 화면: 코멘트 + 매칭/갭분석 점수 변화("개선도"). 텍스트 diff는 목록에 바로 노출되지 않음
- 버전별로 매칭/갭분석 점수를 기록하여 "개선도" 추적 (동일 JD 기준 점수 변화)
- 텍스트 diff: 상세보기 진입 시에만 확인 가능한 보조 기능 (이전 버전과의 정확한 문구 변경 검증용)
- **JD별 브랜치**
  - 슬랙 알림의 "지원 준비하기" 클릭으로 생성 (3.3 참고)
  - 브랜치 생성 시 base 버전은 사용자가 직접 선택
  - 식별 기준은 (사용자, JD) — 동일 JD(소스+원본 공고 ID)에 이미 브랜치가 있으면 새로 만들지 않고 기존 브랜치로 이동
  - 상태 필드(진행중/지원완료/마감)로 관리, 사용자가 수동 변경 및 목록 필터링 가능
  - 브랜치 간 병합(머지)은 지원하지 않음
  - "첨삭하기/피드백 받기" 버튼이 이 화면에 있으며, 클릭 시 그 브랜치가 겨냥한 JD 1개를 대상으로 갭 분석과 첨삭 피드백이 온디맨드로 생성됨 (4.3, 4.4 참고)

### 4.2 공고분석 에이전트
- 입력: 원티드 API / 워크넷 공공데이터에서 가져온 JD 원문
- 출력: 구조화된 JD 데이터
  - 요구역량 (필수)
  - 기술스택
  - 우대사항
  - (기타 메타데이터: 회사, 직무, 경력조건 등)
- LLM: OpenAI API 사용, 구조화 출력(JSON) 강제

### 4.3 매칭/갭분석 에이전트

**(a) 매칭 점수 계산 — 자동, 저비용**
- 입력: 사용자 이력서(특정 버전) + 구조화된 JD 목록
- 처리:
  - 이력서 임베딩과 JD 임베딩 간 pgvector 코사인 유사도 계산 (이미 생성된 임베딩 간 벡터 연산이라 LLM 호출 없이 저비용)
  - 유사도 상위 N개 JD 선정
- 출력: 매칭 점수, 관련 JD 목록
- 이 출력이 발송 로직(4.5)의 입력으로 사용됨
- 채택 근거: 벡터 유사도를 쓰는 이유는 검색 품질의 절대적 우위 때문이 아니다(구조화된 기술스택 태그 기반 키워드 매칭도 상당히 경쟁력 있음). 진짜 이유는 ① 개선도(7번) 정량 측정에 필요한 연속적 점수를 제공하고, ② pgvector·임베딩 기술을 포트폴리오로 시연하기 위함
- **경량 모드 (JD별 브랜치 전용)**: 위 "이력서 1개 ↔ JD 여러 개" 모드와 별도로, "이력서 특정 버전 ↔ 브랜치가 겨냥한 JD 1개"만 계산하는 모드를 제공. JD별 브랜치(3.3, 4.1) 내에서 이력서를 저장할 때마다 자동 재계산되어 개선도 추적(7번)에 사용

**(b) 구체적 갭 분석 — 온디맨드, 고비용 (LLM)**
- 트리거: 자동 실행 아님. JD별 브랜치 화면에서 사용자가 "첨삭하기/피드백 받기" 버튼을 클릭할 때만 실행 (4.1, 3.3 참고)
- 입력: 사용자 이력서 + 그 브랜치가 겨냥한 JD 1개
- 처리: 해당 JD 대비 이력서에 부족한 역량을 LLM으로 구체적으로 분석
- 출력: 갭 분석 결과 — 첨삭 에이전트(4.4)의 입력으로 사용됨

### 4.4 첨삭 에이전트 (RAG)
- 트리거: 자동 실행 아님. JD별 브랜치 화면에서 사용자가 "첨삭하기/피드백 받기" 버튼을 클릭할 때 실행 (4.1, 3.3 참고)
- 입력: 사용자 이력서 + 그 브랜치가 겨냥한 JD 1개(RAG 근거 문서) + 갭 분석 결과(4.3-b)
- 처리: 해당 JD 하나를 근거로, "이 JD 대비 이렇게 수정하면 좋다"는 식의 구체적 피드백 생성
- 출력: 근거(그 JD의 어떤 요구사항)가 명시된 첨삭 피드백

### 4.5 발송 로직 (슬랙 알림)
- AI 로직이 아닌 일반 로직으로 처리
- 트리거 조건: 매칭 점수가 설정된 임계치 이상인 새 매칭 결과 발생 시
- 발송 채널: 슬랙 웹훅 (Incoming Webhook)
- 발송 내용: 매칭된 공고 정보, 매칭 점수, 결과 확인 링크, "지원 준비하기" 인터랙티브 버튼
- **인터랙티브 버튼 처리**: "지원 준비하기" 클릭은 Slack Interactivity 콜백으로 즉시 처리됨 (Incoming Webhook만으로는 불가, Slack App의 Interactivity Request URL 엔드포인트 필요) → 콜백 수신 시 브랜치 생성/이동 로직 호출 (4.1, 3.3 참고)

---

## 5. 데이터 소스

| 소스 | 방식 | 비고 |
|---|---|---|
| 원티드 | Open API (openapi.wanted.jobs) | 개발자 공고 밀도가 높아 우선 채택 |
| 워크넷(고용24) | 공공데이터포털 Open API | 자동승인, 안정적 확보 가능 |
| (추후 확장) | 크롤링 | robots.txt 준수, 요청 빈도 제한 등 정책 수립 후 추가 예정. MVP 범위 아님 |

**JD 식별 기준**: "소스 + 원본 공고 ID"를 JD의 식별자로 사용한다. 같은 회사·직무의 공고라도 소스가 다르거나(원티드/워크넷) 재게시로 새 공고 ID를 받으면 별개의 JD 레코드로 취급하며, 소스 간·재게시 간 중복 제거는 하지 않는다 (엔티티 매칭 기반 중복 통합은 9번 향후 확장 참고).

---

## 6. 기술 스택

| 영역 | 선택 | 근거 |
|---|---|---|
| 백엔드 | Python + FastAPI | LLM/RAG 생태계(OpenAI SDK 등)가 Python 중심, 타입힌트 지원 |
| 벡터DB | pgvector (Postgres 확장) | 이력서·공고·사용자 데이터를 이미 관계형으로 다뤄야 하므로, 별도 벡터DB 인프라 없이 통합 관리. 운영 단순화뿐 아니라, 개선도(7번) 측정에 필요한 연속적 매칭 점수를 제공하고 임베딩 기술을 시연한다는 점이 면접에서 설명 가능한 선택 근거 |
| LLM | OpenAI API | — |
| 인증 | 멀티유저 (회원가입/로그인) | 실제 서비스 형태로 시연 가능 |
| 배포 | 클라우드 무료 티어 (예: Vercel + Supabase 등) | pgvector 지원 무료 Postgres 호스팅 활용, 면접관이 직접 접속 가능 |
| 발송 | 슬랙 웹훅 + Slack App(Interactivity) | 사업자 인증 불필요, 구현 간단. 단, 인터랙티브 버튼("지원 준비하기") 클릭을 즉시 처리하려면 단순 Incoming Webhook만으로는 부족하고 Slack App 등록 및 Interactivity Request URL 엔드포인트가 필요 |

**프론트엔드**: React + Vite (SPA) — 백엔드(FastAPI)와 역할이 완전히 분리되고, 로그인 후 대시보드형 앱이라 SSR 이점이 없어 서버 런타임이 없는 SPA로 결정.

---

## 7. 개선도(버전 간 성과) 측정 기준
- 동일 JD를 기준으로 이력서 버전 A와 버전 B의 매칭/갭분석 점수를 비교
- "이 수정이 실제로 해당 공고 적합도를 얼마나 높였는가"를 정량적으로 제시
- 포트폴리오 관점에서 "단순 텍스트 diff"가 아니라 "정량적 효과 측정"이라는 차별점을 가짐
- JD별 브랜치(3.3, 4.1) 안에서는 "동일 JD"가 곧 그 브랜치가 겨냥한 JD 1개이므로, 브랜치 내 버전들의 점수 변화가 바로 이 개선도 지표가 됨

---

## 8. 오픈 이슈 / TBD

- [x] 프론트엔드 프레임워크 선택 → React + Vite (SPA)
- [ ] 매칭 점수 임계치 구체값 및 알림 재발송(중복 방지) 정책
- [ ] 이력서/JD 임베딩 단위 (전체 문서 vs 섹션별 임베딩)
- [x] 사용자별 데이터 격리 및 권한 모델 상세 → `user_id` FK + 앱 레벨 권한 체크
- [ ] 크롤링 확장 시 대상 사이트 및 정책
- [ ] 슬랙 웹훅 설정 방식 (사용자별 개별 웹훅 등록 vs 공통 채널)
- [ ] Slack 사용자 ↔ 앱 계정 매핑 방식 (인터랙티브 버튼 클릭 시 어떤 앱 사용자의 브랜치인지 인증/식별)
- [ ] 슬랙 내에서 브랜치의 base 버전을 어떻게 선택하게 할지 (버튼 클릭 후 웹앱으로 유도 vs 슬랙 내 추가 인터랙션)

---

## 9. 향후 확장 (Post-MVP)
- 크롤링 기반 JD 데이터 소스 추가
- 이메일 발송 채널 추가
- 버전관리 성능 및 DB 최적화
- JD 엔티티 매칭 기반 소스 간/재게시 중복 통합 (현재는 소스+원본 공고 ID 기준으로 별개 JD 취급, 5번 참고)
- 브랜치 병합(머지) 기능
- 채용 트렌드 분석 기능
  - 개인화된 트렌드 갭: 관심 직무에서 최근 많이 요구되지만 본인 이력서엔 없는 기술 Top N (갭분석·JD별 브랜치 인프라와 연계)
  - 직무별 기술스택 트렌드 대시보드: 특정 직무의 기술스택 언급 빈도·변화 추이
  - 주간/월간 트렌드 요약 리포트 (기존 슬랙 발송 인프라 재사용)
  - 대부분 SQL 집계(GROUP BY/COUNT, 기간별 비교)만으로 구현 가능해 LLM 호출 없이 저비용으로 제공 가능

---

## 10. 비용 추정 (LLM API 비용)

각 구성요소는 실행 빈도가 서로 달라서 비용 성격도 다르다. 아래는 저가형 모델(mini급, 입력 $0.15/output $0.60 per 1M 토큰 기준) 가정 하의 개략 추정치다.

| 구성요소 | 실행 빈도 | 비용 성격 | 건당 추정 비용 | 누적 추정 |
|---|---|---|---|---|
| 공고분석 에이전트 (4.2) | JD 1건당 1회 (수집 시, 재처리 없음) | 수집량에 비례, 예측 가능 | 약 $0.0004 (입력 ~1,500 + 출력 ~300 토큰) | 5,000건 누적 시 약 $2 (수천 원) |
| 매칭 점수 계산 (4.3-a) | 이력서 저장·JD 수집마다 자동 | LLM 호출 아님(임베딩 벡터 간 pgvector 연산) | 사실상 무시 가능 (임베딩 생성만 이력서 버전당 1회·JD당 1회) | 수집량과 무관하게 무시 가능 |
| 구체적 갭 분석 (4.3-b) | 사용자가 브랜치에서 "첨삭하기/피드백 받기" 클릭 시 | **사용자 수 × 요청 빈도**에 비례 | 약 $0.0006 (입력 ~2,500 + 출력 ~400 토큰) | 클릭 1,000회당 약 $0.6 |
| 첨삭 에이전트 (4.4) | 위와 동일 트리거 (갭 분석과 함께 실행) | 사용자 수 × 요청 빈도에 비례 | 약 $0.0009 (입력 ~3,000 + 출력 ~700 토큰) | 클릭 1,000회당 약 $0.9 |

**핵심 판단 근거**: 공고분석·매칭 점수 계산은 "누적 수집량"에만 비례해 상한이 예측 가능하고 저렴하므로 자동 실행을 유지해도 된다. 반면 갭 분석·첨삭은 "사용자 수 × 요청 빈도"로 곱해지는 항목이라 자동 파이프라인에 포함하면 사용자가 늘수록 비용이 선형 이상으로 커질 위험이 있다 — 실제로 활성 사용자 100명 × 신규 JD 5,000건에 자동으로 갭분석+첨삭까지 돌렸다면 약 $750(수십만 원) 규모가 됐을 것을 온디맨드 트리거(3.3, 4.3-b, 4.4)로 전환해 사용자가 실제로 요청한 만큼만 비용이 발생하도록 설계했다.